<% provide(:title, 'All events') %>
<h1>All events</h1>

<%= will_paginate %>

<ul class="events">
  <% @events.each do |event| %>
    <li>
      <%= link_to event.title, event %> - 
      <%= link_to event.creator_id, event %>
    </li>
  <% end %>
</ul>


<%= will_paginate %>